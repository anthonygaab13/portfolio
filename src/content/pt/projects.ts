import { buildProjects } from "../shared/merge";
import type { Project } from "../types";

// Placeholder — nenhum projeto cadastrado ainda.
// Para adicionar: 1) criar a entrada em content/shared/profile-meta.ts (projectsMeta)
// 2) adicionar o texto traduzido aqui e em content/en/projects.ts
export const projects: Project[] = buildProjects([]);
