import { buildProjects } from "../shared/merge";
import type { Project } from "../types";

// Placeholder — no projects registered yet.
// To add one: 1) create the entry in content/shared/profile-meta.ts (projectsMeta)
// 2) add the translated text here and in content/pt/projects.ts
export const projects: Project[] = buildProjects([]);
